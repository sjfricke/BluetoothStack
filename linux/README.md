# Linux stack

> Includes, but not limited to Ubuntu laptop, Raspberry Pi, etc

- Grab libbluetooth
    - `sudo apt install libbluetooth-dev`
- Build Cmake
    - `cmake -Bbuild/ -H.`
- Build Examples
    - `cmake --build ./build/`
- Profit